# 2026-03-10

https://meet.jit.si/CargoTeamMeeting

Out:

Attendees: Eric, Ed Page, Jacob, Arlo, Scott, Weihang, Ross, Josh, Tomas Sedovic, Nurzhan Saken, Dongpo

## FCP Reminders

- build-std: explicit dependencies -- https://github.com/rust-lang/rfcs/pull/3875 (0xPoe LawnGnome Turbo87 carols10cents eth3lbert joshtriplett mdtro weihanglo)
- build-std: always -- https://github.com/rust-lang/rfcs/pull/3874 (joshtriplett the8472 weihanglo)

## New RFCs

- [Custom lint profiles](https://github.com/rust-lang/rfcs/pull/3926)

Ed: It's a counter-RFC to the one I wrote. It's an alternative to build warnings. Requires a recompile when you switch the profile, unlike the nits RFC which doesn't. My concern is complexity.

Scott: There's probably a way around the recompile.

Ed: I don't want to rewrite Rust lints. It would require another lint level to ??. That was one of the key design decisions around lint warnings and erroring out on those.

Scott: If we rewrote them, it would make ?? easier.

Ed: We tried a rewrite, but it turned out to be hard??.

Ed: We're talking about changing the lint level, e.g. from warn to allow.

Ed: Another problem is that the changed lints interact with lints in the source code. This makes it harder to avoid recompiling.

Josh: I can imagine making this changeable, but it would require a massive RFC. Changing between warn/allow is the cleanest case. Anything involving deny/forbid is harder if not impossible that going from warn to allow. The compiler needs to know when to enforce forbid.

Ed: Besides that, it adds issues with inheritance across profiles. Inheriting workspace override would have to be a separate RFC.

Scott: The example is "I'm a workspace member who inherited the workspace lint table, and I want to override the lint level from warn to allow"?

Ed: Yes.

Scott: Inheriting is interesting because we had a similar idea (workspace inheritance?)

Ed: Having groups of workspaces you can inherit is valuable. But this adds a lot more complexity than that.

Scott: I was about to ask you about those comments today, so thank you.

Josh: +1 for the constraints youve mentioned. I agree that inheritance+override would require an RFC. Even with having to recompile, I find the concept of this RFC appealing.

Ed: This reminds me of stuff certain language researchers trying to push Rust to cases where they want generalized solutions, but is the generalization worth the complexity/design quirks? There's a couple of use cases I can see, but they can be solved with something else. For example, let's have stricter lints at release time, which I think is an anti-pattern.

Arlo: I can see allowing warnings during experimentation but not release. This talked about potentially replacing this.

Ed: `build.warnings = "allow"`

Ed: That wasn't clear to me that the RFC is supposed to replace this. But one of the main design considerations for build warnings was to not having to recompile anything.

## build-std unresolved questions

- https://github.com/davidtwco/rfcs/blob/build-std-part-three-explicit-dependencies/text/3875-build-std-explicit-dependencies.md#should-cargo-metadata-include-the-standard-librarys-dependencies

Ed: The RFC says that cargo metadata will start inclusing build-std deps including implicit dependencies.. Part of me thinks this is an easy solution. The problem is where does that fall on our compat spectrum? We're allowed to add package id field format. Someone currently parsing the Cargo metadata today would have to deal with a new metadata fields and decide how to handle them. This felt like a compat hazard warranting an explicit conversation.

Eric: Can you say moe about why this would need an update to Cargo metadata?

Ed: This would fail parsing the package id ??

Arlo: ?

Ed: The parser hardcodes itself to different schemes.

Scott: Is this a ome-time thing?

Ed: Yes.

Scott: In theory, if we were to do this, a one-time cost would be less of a pain.

Ed: Cargo users would need to update their tools or risk breakage. Every tool that uses Cargo metadata would have to be updated and could cause breakage.

Eric: Still not following on what things will be parsing the package id.

Ed: It's no longer opaque. (reads the specification) At the moment they're not parsing the package id spec. Are there other ways it shows up in the ?? output?

Scott: ??

Eric: The source is supposed to be opaque.

Ed: They represent it as a string where it has ?? crates.io on it

Ed: If someone is using `cargo-metadata`, it will not parse. If they use `cargo_utils_schema`, then it will be less of an issue. ?? Maybe the impact is going to be fairly small.

Eric: Did you have concerns besides compatibility?

Ed: No. I'll respond with info on how to avoid compatibility errors to include in the RFC.

## hints.min-opt-level

https://github.com/rust-lang/rfcs/pull/3924

Josh: We talked about this idea before with hints and what it would look like. I put together this RFC, and I appreciate the comments covering many corner cases, e.g. handling "s" and "z" I believe I've covered most cases, and I wanted to ask what would be the next steps. I can provide more background.

Eric: This talks about the hints table, but then there's the hints-?? table. Is it unrelated?

Josh: This is designed to bubble up from your deps to yourself interms of "you're optimizing this particular crate for this opt-level, and then you can override it via profiles". For ?? we need to pass this option and override it per dependency. There are other things that could be added if you need more granularity (min-opt-level, max-opt-level), but I left it out for simplicity.

Ed: How much is this actually going to be used? Have we reached out to packages to get their input, e.g. SQLx and Bevy?

Scott: sqlx macros use this.

Josh: I can reach out and ask for feedback.

Ed: We should involve the audience in the RFC process.

Josh: I'll reach out to Alice from Bevy and someone from sqlx.

Scott: Something I'd like to see is: don't automatically set the optimization level in but suggest which ones to use. It's easier if you control everything in your build. It should let you opt-in.

Ed: Something to keep in mind is that for some people, having to set this in for everything in the dependency tree, especially when it involves transitive deps, it might really heavy. That might be an issue with switching it to opt-in.

Scott: I meant making it configurable, so that if a user wants to turn it off, and give them an option to see which ones they want to use this for. It should still be on by default.

Ed: Designing this config would be complex. Should it be part of the MVP? Or should we ask the users first?

Arlo: We should keep it in mind but not include in the MVP.

Josh: Agreed. If someone wants to opt out, we have a mechanism for that. The mechanism you're describing would be a step past that. "I want to manage the opt-level for individual items while getting notified of the differences"

Ed: How would this interact with debugging? You'd have to override the opt-level for the affected package.

Josh: Or all packages, e.g. something like `[profile.*] opt-level = 0`.

Josh: We've talked about having separate profiles for dev and debug. If we went that route and introduced the debug profile after this hint exists, I'd expect the debug profile to maybe override every opt-level to 0. But for some things this might be undesirable because the user wouldn't want to debug them.

Scott: Is there a reason someone would want to hint for a different profile in e.g. release.

Josh: If you want that level of control, you'd be better off overriding it per dependency.

Arlo: ?? not doing anything.

Ed: That's why we decided on going with *min* opt-level.

Josh: I couldn't think of a use case for max-opt-level. Even in release there's limited value in opt-level 3.

Arlo: Compiler optimizations keep changing too.

Ed: You shouldn't rely too much on the internals of the compiler.

Weihang: This is a hint, but it's something dependencies set and people will have to override almost always.

Ed: This is like hint mostly unused. We could change this or even remove it. It's not a breaking change. The system might actually not use the hint. mostly_unused turns into a ?? inline flag.

Weihang: We reserve the right to not pass any opt-level.

Ed: Yes, we could even remove this change completely.

Scott: Would there be a way to figure out what's been hinted? "This dependency has been set to this opt-level"

Ed: No, there's nothing that has this information per ?? level.

Scott: I feel like people would want to have this for audit purposes.

Ed: There's the profile info on a per-unit basis. We could provide ?

Scott: If a package isn't in your lockfile, and you set it to a opt-level, does it warn you against setting it for a non-existent package?

Ed: Yes.

Eric: Do we want to move this to FCP?

Josh: First, it would be appropriate to reach out to people from Bevy and sqlx, ask them for comments, and only then start an FCP.

Scott: I talked to Jakub who did cargo wizard if we should collect this information and show it in cargo-wizard.

Josh: Any suggestions for other major crates apart from Bevy and sqlx? I see crates in the ecosystem that ask to set the opt-level, but..

Ed: So far the only one I found through searching for "opt-level" on crates.io is https://crates.io/crates/google-cloud-iot-jwt which suggests setting opt-level z.

Josh: ?? Just because something is designed for embedded, it doesn't mean you'd want to opt it for size or speed.

Ed: The [rsa](https://crates.io/crates/rsa) crate suggests setting opt-level 3 for its dependency.

Scott: I was wondering about cryptographic crates setting this for safety reasons

Josh: Reaching out to 4/5 of these and directing them to the thread should be sufficient.

## Signing goal

Updated goal: https://hackmd.io/lQpkpPpCSTqlJMK1i6PIAw

Goal was reworked to focus on concrete deliverables. Focus is intended to be on rustup. Some risks around the rustup team's bandwidth and ability to participate. Arlo gave an update on Merkle tree experiments.

Eric: I've read it but haven't given feedback.

Ed: The summary talks about rustup only, but then also mentions Arlo's crates.io research.

Eric: Yes, needs some cleaning up.

Ed: Would like Arlo's merkle tree to be a separate goal.

Arlo: I'd be okay with doing a separate merkle tree crates.io goal even without signing.

Ed: One is concrete work and the other is more of a Foundation staff research project.

Jacob: How do we navigate our relationship with Foundation staff? Walter and crew have their own Merkle tree impl.

Arlo: We could come up with a subgoal with only the merkle tree stuff. I'm still leaning to keeping them together, first targeting rustup, and crates.io being an add-on.

Ed: The goal's target says it's to get signing going for rustup *and* do a research project crates.io. To me it seems like it could be done as two separate goals. Having "and" in the title of the goal suggests it could be two goals.

Jacob: The counterargument that supports opening the goal again is that there's a lot of infra that has changed since the last promise of regular updates. If there were a goal where Tomas is in charge of getting updates from people, ??

Ed: It's helpful to have nudges, but nagging feels like a problem.

Josh: We should set that as a prerequisite. Development should be happening in a public repo, and we could set it as an explicit requirement.

Jacob: Eric, do you have updates from the council on this?

Eric: Not really. It's hard to figure out the actionable steps. There's a back and forth without clear requirements from the Foundation. It gets muddled because we're not their manager.
