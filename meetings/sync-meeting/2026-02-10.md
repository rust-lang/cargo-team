# 2026-02-10

Out:
Attendees: Eric, Scott, ada, Weihang, Arlo, Ed Page, Ross, Dongpo Liu, Josh, Mohamed Ali, Eh2406, Cyborus, Tomas Sedovic, Nurzhan Saken

## FCP Reminders

- build-std: always -- https://github.com/rust-lang/rfcs/pull/3874#issuecomment-3715844925
- build-std: explicit dependencies -- https://github.com/rust-lang/rfcs/pull/3875#issuecomment-3715845871
- cargo script -- https://github.com/rust-lang/cargo/pull/16569#issuecomment-3824367099



## rust-lang/cargo review queue

Do we want to try the new triagebot review queue? https://rust-lang.zulipchat.com/#narrow/channel/246057-t-cargo/topic/Review.20queue.20tracking/with/572835921

Eric: triagebot queue is live and working, wondering if we want to use it on cargo repo. You can limit the amount of PRs you have assigned to you. If anyone wants to join the queue, it would be nice to have more people doing reviews. You can just sign up for one slot to try it out.

Ed: we need to be more strict on and sticking to only PRs assigned to us. I haven't looked into how this works, but being able to pause the review queue would be good. Will be having limited availability in a few months.

Josh: you can mark yourself on vacation with rustbot.

Weihang: Does it support a code owner? By default e.g. Scott gets assigned all the lints.

Scott: I wish it would do reviews plus one. I can review lints easily, but it wouud be great to have someone else there too.

Eric: I don't think it will be hard to switch over. I'll have J switch it and we'll see how it goes

## rustdoc mergeable info

https://github.com/rust-lang/rust/issues/130676#issuecomment-3873775353
Request for feedback for stabilization.
docs: [rustdoc](https://doc.rust-lang.org/nightly/rustdoc/unstable-features.html#--merge---parts-out-dir-and---include-parts-dir) [cargo](https://doc.rust-lang.org/nightly/cargo/reference/unstable.html#rustdoc-mergeable-info)

Eric: there was a request for comments on stabilization of  rustdoc mergeable info...

Ed: ... have different levels of/reqs for rebuilds, idea: would be great if rustdoc generated json for diff units, and at the end linked them into... like rustc? .. Bjorn suggested generating the html in the mergable location and reflink to the final locatino. This is something that well need to work out is if we need to go that route

Weihang: we should confirm with rustdoc if this is feasible, .. could make it not feasible

Eric: cross-crate links checks whether files are still there

Weihang: i thought this was handled

Eric: afaik it doesn't modify the html files

Weihang: ask the rustdoc team whether it's possible, then me or someone else can experiment, or we accept the current state and merge it

Eric: is there, can we move incrementally, start using this and switch to a more sophisticated system later?

Ed: they'd be stabilizing throwaway work, what's the cost of this being throwaway feature?

Josh: the goal is incremental build arts for each crate ... rather that building all at once

Ed: for rustdoc it's fixing problems with building search. For us, because of how info is being tracked, there's lots of risks with rebuilding bugs. Operating like rustc .. but caching a lot more better

Eric: next steps - continue that conversation, wait until they respond

Eh: important piece - people, how do we respect their ownership while picking somethnig that works for us, i dont know how to balance those concerns


## unused dependencies

https://github.com/rust-lang/cargo/pull/16600

Topics
- Would appreciate someone familiar with compilation to have a look over
- Check roots only (current) or any local package that happens to have exhaustively covered dependency kind (more bookkeeping)?
- Approach to false-positives:
  - config on the lint
  - `_` prefix like variables/functions

Ed: picked up work on unused_depend, force enable and not show info to users, harvest that info and if you've exhaustively gone throught all the dependencies, I'll emit a flag for that ... because we dont have -all-crates flag.. we can't do this checking for dev-deps because theres no way of exhaustively building all dev-deps.

Ed: I don't know who's familiar with compilation side of things. Eric, do you have exp with ..?

Eric: yes, I can work with that

Ed: having a track for unit dep -- what manifest dependencies does it track

Ed: right now, it only checks roots or selected packages. If you're in a workspace and you build cargo, you'll see the lints for cargo, but also cargo-utils. With this, you'll see the cargo lints but not cargo utils. It only checks whether all targets were exhaustively enumerated or not. we do it across all roots because that's trivial (?)

Ed: if you do a cargo check, that will .. but if you also

Ed: wanted people's thoughts on if we check roots, but not local packages. If you check a library, it will only check the library. I wanted to be sure people are on board with that.

Scott: would be nice to break this down in a table.

Ed: I can bring a table next week

Arlo: what's the actual thing whose output you care about?

Josh: in terms of difficulty, dev-dependencies feel like a lower priority than actual dependencies.

Ed: that's a good thing to double check, the lint is viable for normal dependencies because dev-deps only affect you. Normal dependencies have more of an impact.

Josh: In dev-dependencies, I need to see anything using dev dependency that might depend on us.

Josh: forgetting artifact dependencies for the moment, binaries are only a leaf. If you're building a binary, you're building a local crate.

Ed: if you cargo check --bin, we will not report unused deps, even if that's the only bin.

Josh: If it's 1 bin, it might be useful to report.

Ed: What we track now is: was the build target selection exhaustive?

Ed: If you have a local dependency that's only a lib, that's reported? Right now it doesn't Right now you have all the build targets, even .. if you do cargo check -p cargo, you'll see lints for cargo-utils. But here we won't. Should we be smart enough to show these lists too?


Arlo: how are we going to explain this in the docs in a way that makes sense to users? "This lint will opportunistically work if these are true" is more complicated ??

Ed: True, the current logic might be easier to document.

Ed: current behavior is easiest, most consistent and predictable. Lints are changeable, few stability guarantees except the name

Josh: a safe strategy is to start with simple, make more sophisticated later. The lint is more important for cargo run than ..

Josh: I'm thinking of the case when you have more than one packages.


Ed: it's probably better to do cargo run -p because cargo run --bin --workspace causes feature unification across bins?

Ed: false-positives, we have a dep on openssl-sys for the sake of enabling a feature on openssl-sys. There are valid cases for false positives.

Ed: one approach is to have a lint config for cargo [[lints]] that..
mimic Rust - if you have an unused var, you rename a package with `_prefix` and it will automatically be unused

Scott: having lint configuration would be great before stabilization (it'd be good to have one thing we could change), I don't think the underscore makes sense. Being able to say this is false positive would be great. Thats's what cargo-machete and cargo-udev do (they check for unused dependencies)
https://github.com/bnjbvr/cargo-machete?tab=readme-ov-file#false-positives

Ed: How do they handle false positives

Scott: They have a table. They do have fales positives: https://github.com/bnjbvr/cargo-machete?tab=readme-ov-file#false-positives

Eric: you should be able to put an extern crate .. to silence the lint

Jacob: It makes sense but sounds horrible.

Josh: We should have a way to explicitly say that a dependency is used

Ed: I wanted people's opinions on where to steer.

Josh: I think it should be on individual dep entries, rather than in a separate table in a list

Ed: that will be complicated for us to scale the existing manifests to add an entry for each lint. because Rust has a general syntax for suppressing lints, even if it's not ideal, we'll have to do something different here

Josh: flagging that unused is smth pervasive in Rust, machinery in compiler for recursive unusedness checking, that's a reason for special syntax with `_unused`

Josh: for this one specific case of unused, it needs some syntax to mark something unused locally, not in a place disconnected from the dependency table

Scott: could you add a field to dependency like unused = false?

Ed: That's what Josh proposed. It feels too specialized. At what point does it justify the lint

Scott: if you inherited the table from the workspace, you can't .. table. That's better but we have to resolve that behavior.

Arlo: this might be a situation where we need both systems for suppressing lints, a general and specific

Ed: `_prefix`: we rejected smth similar for features. rustdoc considers `_features` hidden, we considered it unacceptable. One aspect of that that makes sense but weird is a kebab-case name needs to be renamed to `_snake_case`.

Scott: a lot of people when they add an unused dependency would like to add a comment -- a reason for doing that. Adding just `unused = true` would be ok, but having unused + reason is a lot of extra syntax to add to the dependency, which is why solving the lints table pronblem makes more sense to me

Josh: used=true or used=string (no separate field) could work. Can you do that in the short form too?

Ed: Yes. `used.reason = ""`

Josh: `foo = { used.reason = "", ... }`

Scott: and now you can also add multi-line tables

Josh: distinguishing it that way works. use.reason ??

Ed: if we go with used and extending the dependencies table, does it need an RFC?

Josh: does this info need to be on crates.io in the index?

Ed: no?

Josh: it would just be a Cargo RFC, feels like you could do an RFC or a PR with a detailed description since we had the design discussion here.

Ed: our general rule if you change manifest, you need an RFC

Eh2406: might make sense to ..

Josh: thank you for working on that

Ed: This is going to overall improve build-times in the ecosystem.


## cargo-script stabilization

> If people share their build-dir across projects, then multiple scripts will point to the same Cargo.lock, fighting over what is locked in it

> Not allowing local config for scripts could become a problem as we get into workspace support as people would expect their repo's config to be respected when it won't be

Config change
- https://github.com/rust-lang/cargo/pull/14749
- https://github.com/rust-lang/cargo/issues/12207#issuecomment-2358818671

Ed: we didn't have time last week for this.

Ed: we currently keep the lockfile in the build.dir. Not documented as best practice, but we supported users .. who have a shared build dir across the system while randomly using some parts of that

Ed: we could say "that's the cost of a shared builddir, untill Ross builds the shared cache will help replace that problem"

Ed: another thought: to not couple the lockfile to builddir, look something like the build-dir so we generate lockfile path specific to that so we guarantee when

Ed: do we have about this case? If so, does it seem like a reasonable solution?

Arlo: already problems with shared cache in the build dir

Ed: different sets of features will get different .. in the cache

Ed: now that we have build dir, we don't have stomping .. having a shared build dir is going to be lot less problematic than before

Josh: There's precedent for cargo script doing it differently than the standard cargo invocation, insofar as our handling of cargo config. Doesn't seem unreasonable for it to behave differently. Not a big deal if it does, don't worry too much, ship, and we'll build a better shared cache feature later

Eh2406: I'd argue the opposite. If we ship the cache first.. if you want to use the new functionality, use this new shared cache feature we built. If we do it the other way around, that's going to be painful.

Arlo: if the current situation continues, that'll be unfortunately. I wouldn't make this worse..

Ed: I'm interested in the historical perspective

Eric: historically, if it works for you, it does, but we never promoted it. It's not something we emphasized. Q: how difficult to put it in separate directories? Would it be a one-line change?

Ed: It's a one line change, do a ?? hash. Regardless of what they do with the build-dir, that will make it unique.

Eric: I would slightly lean towards being able to differentiate them, but I don't feel strongly about that.

Eh2046: I as a user expect to not care about where the cargo lockfile is. But I don't want it to spuriously rebuild.  If I don't know where the lockfile is, I lose context for the other two.

Ed: I'll go ahead with doing the simple change of adding a redundant .. into .. so that people have control over the location but it wouldn't cause conflicts.

----

Ed: the original RFC called for loading the config relative to the script.

Ed: if you do cargo-install for the path package, it will reload the config to ??

Ed: for cargo script I decided to reload ... cargo install path ..

Ed: at some point in the conversation behavior concerns came up about .. directory, my main concern is: the behavior of this when it comes to scripts in a workspace. If I have scripts and config in my repo, it would be weird .. .. if you do cargo run it'd work, if it's a normal package it would work.

Ed: that's a contrived situation because you're not accidentally downloading a config file, you're creating a directory .. the main situation would be: you unzip something with a top level directory into your downloads folder. And given our standard approach to this, we've given a pass on things like this.

Josh: 2 thoughts. We don't need to treat it from a security sensitive aspect. If you're donwnloading a script and running it, the security depends on the script. If you're downloading a config, it can change things. This shouldn't be a security but user expectation/surprise issue.

Josh: We should respect the config of a workspace in the presence of a script.

Josh: I have a script in my bin directory, and I'm running it standalone, and I don't want to search for an unrelated config in .. directories.

Josh: Can we tell the differencty by saying: if you're a part of a workspace

Ed: That's too late. For Cargo script we know a special configuration that we load from

Josh: the proposal to act like cargo install and getting ?? from a known location rather than doing the search upwards thing.

Ed: cargo install does an upwards search starting from the current directory.

Josh: but not from the current directory ??

Arlo: similar point: two cases in tension where a script is independent or a part of a workspace.  I don't think tihs is necessarily a script problem. We should continue to research and limit the search upwards approach not just for cargo script but in general.

Ed: E.g. have a stopgap where you don't go anywhere above `bins` if you have that directory

Josh: It woulde be nice to have some means of embedding the the config into the script. It'd be annoying to lose self-containedness just because you want to optimize for a given target.

Ed: We have an issue for finding the right approach for adding config into the manifests.

Josh: That sounds good and better than shiping a second manifest.

Weihang: Wher should we read things from? Should this be a blocker for stabilization?

Ed: changing this would be a breaking change, so this is a blocker fror stabilization.

Weihang: should we not download any config file?

Ed: we're doing this today. We load from ?? only. .. As people use this more .., once we get workspace support, people will want their config apply to the workspace

Weihang: we could have a table at CARGO_HOME ..

Ed: that would be messy to have to read the cargo home. We'd have to reload the config only to read cargo home, which would result in more reloads and constant overhead

Josh: we should pick something that makes the most sense for the most common use case, ..

Josh: if there's some way to design a scheme to evolve this in a non-breaking fashion, .. seems like we have problems with .. and this shouldn't be blocking people ..

Ed: I'll put up a PR to switch back to the ?? location. If it's a bad experiencee, it's the same bad experience people have today.
