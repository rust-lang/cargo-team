# 2026-04-14

https://meet.jit.si/CargoTeamMeeting

Out:
Attendees: Eric, Ed Page, Scott, Arlo, Dongpo Liu, Ross, Weihang, Josh, Nurzhan Saken

## FCP Reminders

- Stabilize build-dir layout v2 #16807 -- https://github.com/rust-lang/cargo/pull/16807
  - 0xPoe, Eh2406, Muscraft, arlosi, ehuss, joshtriplett
  - **beta branched, ideally merged asap**
- feat(compile): Stabilize build.warnings #16796 -- https://github.com/rust-lang/cargo/pull/16796
  - 0xPoe, ehuss, weihanglo
- RFC: Inheriting of default-features in Cargo#3945 -- https://github.com/rust-lang/rfcs/pull/3945
  - 0xPoe, Eh2406, arlosi, ehuss, joshtriplett, weihanglo
- Add -m shorthand for --manifest-path#16858 -- https://github.com/rust-lang/cargo/pull/16858
  - 0xPoe, Eh2406, joshtriplett

## New RFCs

## Apparent race condition between different versions of Cargo #16853

https://github.com/rust-lang/cargo/issues/16853

Eric: Wanted to see what's going on since looks like a beta regression.

Ed: We acknowledged this can happen. Part of the thinking was when the lock was theoretically advisory... I haven't considered utilities linking against cargo being another version... I'd be interested in thoughts on how much effort we should put into this. When we stabilize new builder layout, this should go away (old and new layout shouldn't stomp on each other).

Eric: Why the filename change?

Ed: We're trying to lock the build-dir and artifact dir separately as part of having fine-grained locking eventually. We originally had extra code for smart locking, but it meant running into this situation. Alternative is creating a separate lock we switch to, and the old lock should be taken by other stuff (?).

Josh: In the new world, two instances of cargo would lock the relevant parts of the directory and lock out the old versions of cargo entirely?

Ed: Yes. Should be easy to add the extra lock there. We'd be able to transition to the current set of locks (2) when we stabilize the new layout.

Weihang: Does it mean our policy on supporting linking cargo changes?

Ed: the main annoyance that kept me from linking againt cargo is manifest... and I get warnings about fields being unused. Hard to predict other problems with linking against cargo. We try to make it work seamlessly.

Weihang: API constantly changes.

Ross: artifact dir doesn't change. If we have a new lock, we might have to keep holding onto the old lock

Ed: Are you talking about conslidating the two locks?

Ross: ??

Ed: We'd be back to the original situation then.

Josh: Even once we released, we have to deal with the possibility of people using two cargos concurrently.

Ross: So in the end we'd have .cargo-lock and .cargo-build-lock, no cargo artifact lock?

Ed: We'd have to decide. Since it's low effort might go ahead.

Ross: I'll take care of the PR.

## Repo status check with `package.include` and `.gitignore`

Eric: Two new scenarios found:

1. https://github.com/rust-lang/cargo/issues/16872
    * uncommited file not inside `package.include` considered dirty (and is ignored by `.gitignore`)
2. https://github.com/rust-lang/cargo/issues/16873
    * nested git repo in `package.include` considered dirty (and also include the entire `.git` directory)

The team has discussed repo status check is considered advisory a while ago.

Weihang: I think the issue is that they have a venv directory, and it's not specified in package.include. But ?? is identified as ?? and fails the build. IMO it's reasonable to ignore ?? not specified in package.include.

Josh: Trying to understand (1). Include in cargo.toml is anchored at the top of the package, not a generic name. It's not like gitignores, right?

Ed: Yes, they're paths or globs.

Josh: If that's the case, then this file shouldn't be included because neither the directory nor the file is inlcuded anywhere.

Arlo: Is it getting picked up specifically because it's LICENSE? Or because of gitignore?

Weihang: It sees it as dirty because ??. This is not documented in cargo doc. It's kind of reasonable to ignore everything outside of package.include. There might be something that's linked through something else than package.include. Maybe that's a user error and we should say people shouldn't do that.

Another proposal is using dep-info to be precise, but that's tricky.

Ed: It's an idea, maybe not a good one.

Weihang: The second one, it's about nesting inside package.include... This one I feel like we should say, use allow-dirty or not use package.include...

Eric: (About using `!` inside package.include.)

Weihang: We could not do anything here since it's an advisory check.

Josh: If you put a leading slash on it it should fix that. This is, I think, someone getting surprised that writing `LICENSE` means any file named that in the package rather than directly under root.

Arlo: Is it using gitignore format for this?

Ed: ?? written by BurntSushi to support gitignore.

docs: https://doc.rust-lang.org/cargo/reference/manifest.html#the-exclude-and-include-fields

Arlo: Then this is working as expected.

Josh: This should be handled via docs and... (a warning about what the pattern catches?)

Eric: ??

Josh: If you're doing include with a glob, you mean...

Eric: Another example is if you have editor temp files (~ files), you may not want to include those, but with a glob they'll get included.

Josh: I wonder if we could offer an option to respect gitignore or change that value over an edition?

Ed: Why can't they ??

Eric: In my gitignore maybe I have a hundred entries, but Cargo doesn't know anything about those, so the safety net is gone.

Josh: This'd apply to any kind of thing you'd want ignored.

Ed: Why not say the user do git(?) without package.include? To reduce duplication?

Josh: Duplication of what's in gitignore. Partly it's reducing surprises.

Ed: I think we should stay separate from gitignore. It's our fallback. Part of the reason is VCS integration. What about jjignore and the rest?

Weihang: When using package.include, we just opt-out of any other heuristics?

Josh: We already do that. The issue is that this behavior is surprising. To Ed's point, we could rely on whatever VCS the project is using.

Ed: This then turns into not just duplication but mismatch between what the VCS and cargo considers dirty.

Eric: Even if you do allow dirty, it's going to pick up the ignored file and the package.

Josh: It will let ?? proceed but include the files you wanted excluded.

Ed: Should we be independent of git or integrate with it? If you include something your VCS excludes, what's the expected behavior for that?

Josh: gitignore patterns are default excludes, and include patterns are default includes. What happens when you include a glob and exclude a directory matched by that? We could make them mutually exclusive and ask users to explicitly use a `!`.

Ed: I used that, and it gets messy to try swapping the polarity of gitignore. There are limitations to how git handles that.

Josh: Something being excluded with a `!` doesn't mean you want it included, just means you don't want it excluded.

Weihang: Question on (1). Usually people use include license with a leading slash because they want every license file included. Then they can't ignore the venv license file. How to do that smarter if we want people to have their ignore include rule?

Eric: !venv,LICENSE

Arlo: If they want some of the licenses but not all, they'll have to encode that somehow.

Josh: I suspect the desired behavior would be processing all includes, then all ignores.

Ed(chat): gitignore, dirty:
https://github.com/rust-lang/cargo/issues/8407
https://github.com/rust-lang/cargo/issues/12294

opt-in to non-vcs logic
https://github.com/rust-lang/cargo/issues/16267
 
Ed: (Concern about generated files.)

Arlo: I wonder how much this can be solved with better diagnostics/suggestions.

Josh: If people will get creative and we want to allow that, the model needs to provide an opt-out. Based on edition, we could change the default behavior and let users opt-in/out. I wonder if we can write this off until someone comes up with a specific contrived scenario.

Weihang: ... if they're okay with it, we can close the issue and link to somewhere.

Scott: Josh's idea could be specified in the manifest...

Josh: I was suggesting it be in the manifest, not config. The name should avoid mentioning git because we want to support any VCS.

Arlo: If this is an uncommon case and we can direct people to ways to encode this, I don't think it's a major issue.

Eric: +1. Normally, I'd prefer to publish in CI.

Ed: So far I haven't found a good CI-based release workflow. I think it's still fairly immature across the ecosystem.

Weihang: I can write a suggestion to the issues and come up with an old issue about Josh's idea on include respecting gitignore.

Arlo: If we could make publishing in CI better, that could be a project goal.

Ed: It's about releasing in CI, where you need to create tags, etc. One case is when development continues in the middle of a release, and you need to release in a branch and merge it back in. There are race conditions in the setup.

Josh: For *publish* from CI, usually I've seen setups that react to published tags, which is unambiguous. *Release* from CI would be more complicated.

Ed: That also depends on issues of error-recovery (partial failure). On complete failure, do you want to re-release that version or burn that and release a new one? There are many moving pieces that make this complicated.

Eric: It seems like one of the issues is a duplicate of an older issue, as Weihang mentioned. The resolution is for Weihang to comment on the issue. We should also add diagnostics.

Arlo: One concern is that diagnostics shouldn't be performance-intensive.

Eric: I'd expect it to only run in the error case.

Josh: Even in the error case I'd only expect it to run across the ignore file.

## Response to TC

Ed: I've been iterating on this and talking to people from the team. Is this something we want to post as a team, or should I post as an individual?

Ed: Is there anything in the response people would be concerned about coming from the team?

Josh: I think we've had a lot of back and forth that's been a bit ambiguous around how much is this the position of the team. It'd be nice if we could provide a unified position. I think it solves more than if Ed posted this alone.

Scott: When do you want to send this?

Ed: Would be ok for you, say, by Friday?

Weihang: How do you want the feedback?

Ed: Call me, DM, but not hackmd comments please.

Eric: Haven't had the chance to read it. Will do this week.

Josh: I read through it with a critical eye. I think that in that regard it's written carefully, but I personally think it's been designed to not be polarizing, so hopefully shouldn't be controversial.

Ed: I used MSRV as an example of decision-making process.

Josh: You might want to start a private thread for people's convenience, maybe at #t-cargo.
