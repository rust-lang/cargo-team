## 2025-01-14

- ehuss: Changelog, where to put it?
    - https://doc.rust-lang.org/releases.html
    - Can put in cargo book
        - Or very similar, just a rendered HTML page at https://doc.rust-lang.org/cargo/releases.html
    - ehuss will look at updating that
    - weihang: is the doc size a problem
        - Book is currently 13MB, release page might add about 500k, doesn't seem to much of an issue
- epage: JSON Schema generation
  https://github.com/rust-lang/cargo/issues/12883#issuecomment-2567241649
  - Tracking config source on a per-field basis makes it difficult to pull out the struct definitions for reuse
  - Could switch to per-lookup tracking
  - Would make the code not as clean
  - Config-relative paths, serde structs need to know about the config loader (tightly coupled).
  - Moving config to cargo-util-schema (to generate), the `Value` wrapper needs to have knowledge of the config back-end.
  - eric: Some config types aren't accessed in aggregate.
      - ed: Could have a cfg for those specific cases.
  - ed: How does merging work, struct or field level?
      - When loading the files, the TOML tables get merged while the files are loaded. Environment overrides are special, and processed when accessed. Environment variables make merging messy.
- epage: should we deprecate and remove `package.authors`?  See https://github.com/rust-lang/cargo/issues/15027
    - authors is currently soft-deprecated, pretty much unused in Cargo, but no warning.
    - Now default removed from clap.
    - Request to have opt-in to set authors in `cargo new`.
    - Proposal: Be more aggressive removing/deprecating in docs, once we have lints generate a warning, then possibly remove it later.
    - weihang: Is author the only one?
        - `[badges]`. Pretty mutch unused.
    - weihang: And the environment variable?
        - No change, deprecating will make people stop using it.
    - No objections. Will also close this issue.
- epage: Topic
